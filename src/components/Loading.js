import React from 'react';
import loading from '../img/image.js';

const Loading = () => {
    return (
        <img className="loading" src={loading} alt="loading" />
    )
}

export default Loading;